<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Product Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
  </head>
  <body class="bg-gray-100">
    <%- include('../includes/navbar.ejs') %>

    <div class="max-w-5xl mx-auto m-12 bg-white shadow-lg rounded-xl p-6 flex flex-col md:flex-row gap-8">
      <!-- Product Image -->
      <div class="flex-shrink-0">
        <img src="/<%= product.pimage %>" alt="<%= product.pname %>"
          class="w-full max-w-sm rounded-lg object-cover mx-auto md:mx-0" />
      </div>

      <!-- Product Info -->
      <div class="flex-1 space-y-4">
        <h2 class="text-2xl font-semibold text-gray-800"><%= product.pname %></h2>

        <div class="flex gap-6 items-center text-lg font-medium">
          <div class="text-pink-600 font-bold text-xl">₹<%= product.pprice %></div>
          <div class="text-green-600 text-base"><%= product.pdisct %>% OFF</div>
        </div>

        <p class="text-gray-700 text-justify text-sm leading-relaxed">
          <%= product.pdesc %>
        </p>

        <!-- Action Buttons -->
        <div class="flex flex-wrap gap-4 pt-4">
          <!-- Add to Cart -->
          <form action="/cart/<%= product._id %>" method="get">
            <button type="submit"
              class="bg-green-600 hover:bg-green-700 text-white px-6 py-2 rounded-md text-sm flex items-center gap-2 transition">
              <i class="fa-solid fa-cart-shopping"></i> Add to Cart
            </button>
          </form>

          <!-- Update Product -->
          <form action="/editproduct/<%= product._id %>" method="get">
            <button
              class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-md text-sm flex items-center gap-2 transition">
              <i class="fa-solid fa-pen-to-square"></i> Update
            </button>
          </form>

          <!-- Delete Product -->
          <form action="/deleteproduct/<%= product._id %>" method="get"
            onsubmit="return confirm('Are you sure you want to delete this product?');">
            <button
              class="bg-red-600 hover:bg-red-700 text-white px-6 py-2 rounded-md text-sm flex items-center gap-2 transition">
              <i class="fa-solid fa-trash"></i> Delete
            </button>
          </form>
        </div>
      </div>
    </div>

    <%- include('../includes/footer.ejs') %>
  </body>
</html>
