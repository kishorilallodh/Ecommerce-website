<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Product Form</title>
 <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4..." crossorigin="anonymous" referrerpolicy="no-referrer"/>
</head>
<body class="min-h-screen bg-gray-100 font-[Segoe UI]">

  <%- include('../includes/navbar.ejs') %>

  <main class="flex justify-center items-center py-12 px-4">
    <div class="bg-white rounded-xl shadow-xl p-8 w-full max-w-4xl">
      <h2 class="text-2xl font-semibold text-center text-gray-800 mb-8">Product Details</h2>
      <form action="/submitproduct" method="POST" enctype="multipart/form-data" class="grid gap-6 md:grid-cols-2">
        
        <!-- Product Name -->
        <div>
          <label for="pname" class="block font-semibold text-gray-700 mb-2">Product Name</label>
          <input type="text" name="pname" id="pname" required placeholder="Enter Product Name" class="w-full px-4 py-3 border border-gray-300 rounded-md "/>
        </div>

        <!-- Product Price -->
        <div>
          <label for="pprice" class="block font-semibold text-gray-700 mb-2">Product Price</label>
          <input type="number" name="pprice" id="pprice" required placeholder="Enter Product Price" class="w-full px-4 py-3 border border-gray-300 rounded-md "/>
        </div>

        <!-- Brand Name -->
        <div>
          <label for="pbname" class="block font-semibold text-gray-700 mb-2">Product Brand Name</label>
          <input type="text" name="pbname" id="pbname" required placeholder="Enter Brand Name" class="w-full px-4 py-3 border border-gray-300 rounded-md "/>
        </div>

        <!-- Discount -->
        <div>
          <label for="pdisct" class="block font-semibold text-gray-700 mb-2">Product Discount (%)</label>
          <input type="number" name="pdisct" id="pdisct" required placeholder="Enter Discount %" class="w-full px-4 py-3 border border-gray-300 rounded-md "/>
        </div>

        <!-- Description -->
        <div class="md:col-span-2">
          <label for="pdesc" class="block font-semibold text-gray-700 mb-2">Product Description</label>
          <textarea name="pdesc" id="pdesc" required placeholder="Enter Product Description" class="w-full px-4 py-3 border border-gray-300 rounded-md min-h-[120px] resize-y "></textarea>
        </div>

        <!-- Image Upload -->
        <div class="md:col-span-2">
          <label for="pimage" class="block font-semibold text-gray-700 mb-2">Product Images</label>
          <input type="file" name="pimage" id="pimage" required multiple class="w-full px-4 py-3 border border-gray-300 rounded-md file:bg-gray-100 file:border-0 file:rounded-md file:py-1 file:px-2"/>
        </div>

        <!-- Category -->
        <div class="md:col-span-2">
          <label for="pcategory" class="block font-semibold text-gray-700 mb-2">Category</label>
          <input type="text" name="pcategory" id="pcategory" required placeholder="Enter Product Category" class="w-full px-4 py-3 border border-gray-300 rounded-md "/>
        </div>

        <!-- Submit Button -->
        <div class="md:col-span-2">
          <button type="submit" class="w-full bg-[#ff3f6c] text-white font-semibold py-3 rounded-md hover:bg-[#e5385e] transition duration-300 cursor-pointer">
            Submit Product
          </button>
        </div>
      </form>
    </div>
  </main>

  <%- include('../includes/footer.ejs') %>
</body>
</html>
